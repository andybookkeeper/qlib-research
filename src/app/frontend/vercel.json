{
  "buildCommand": "npm install --no-optional && npx tsc && npx vite build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
